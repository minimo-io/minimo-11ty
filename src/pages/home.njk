---
permalink: /
description: "Design, prototyping and creation of cross-platform craziness using Figma, Vue.js, Flutter, Woocommerce & SSGs like 11ty"
---

<div class="wrapper article text-justify mt-5">
    <h2 id="summary" class="h3 font-weight-600 Xtext-center">
        🦄 <span class="font-italic">Summary</span>
    </h2>
    <p class="Xfs-400 font-italic Xtext-center">A year-over-year summary of all <a href="/work/">work</a> activities, project launches and articles, involving development, design and... eveything in between!</p>
    <div  class="archive mb-4">
        {% set currentYear = "" %}
        {% set count = collections.posts.length %}
        {% for post in collections.posts | reverse %}
            {% set postYear = post.date.getFullYear() %}
            
            {% if currentYear != postYear %}
                {% set currentYear = postYear %}
                <h2 class="h3 font-italic">{{ postYear }}.</h2>
            {% endif %}
            
            <p {% if post.data.star %} class="star"{% endif %} Xstyle="font-size:var(--fs-clamped-400);font-weight:200;">
                <a href="{{ post.url }}" style="font-weight:200;">
                    #{% if count.toString().length < 2 %}0{% endif %}{{ count }} 
                    - {{ post.data.title }}
                </a>
                {# <span class="pill">article</span> #}
                {% for tag in post.data.tags %}
                    {% if tag != "posts" %}
                        <a href="/tag/{{ tag | slugify }}/"><span class="pill">{{ tag }}</span></a>
                    {% endif %}
                {% endfor %}                
            </p>
            {% set count = count - 1 %}
        {% endfor %}
    </div>

    <div class="mb-4">
        <h2 id="photos" class="h3 font-weight-600">
            🖼️ <span class="font-italic">Photos</span>
        </h2> 
    
        <p class="Xfs-400 font-italic">A collection of public available pictures.</p>
    </div>

    <h2 id="biking" class="h3 font-weight-600 mt-4">
        🚴🏼‍♂️ <span class="font-italic">Biking</span>
    </h2>    
     <p class="Xfs-400 font-italic">Some aventures on top of <a href="/biking/">a bike</a>, on <a href="https://en.wikipedia.org/wiki/GPS_Exchange_Format" target="_blank" rel="noopener noreferrer">GPX files</a>.</p>        
</div>

{# <hr class="my-3"> #}


   

